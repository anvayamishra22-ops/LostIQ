{
  "name": "lost-iq",
  "version": "1.0.0",
  "description": "Lost & Found Management System (LOSTIQ)",
  "scripts": {
    "install-backend": "npm install --prefix backend --legacy-peer-deps",
    "install-frontend": "npm install --prefix frontend --legacy-peer-deps",
    "build-frontend": "npm run build --prefix frontend",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
    "vercel-build": "npm run build",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
